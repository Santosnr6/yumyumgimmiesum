export const toUpper = (value) => {
    return value.toUpperCase();
}


// funcition takes array and creates a string with all the elements separated by a comma but not after the last element
export const formatIngredients = (ingredients) => {
    return ingredients.join(", ");
}